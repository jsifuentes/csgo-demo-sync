import { Box } from '@material-ui/core';
import React from 'react';

// const useStyles = makeStyles({
//   root: {},
// });

export default function RoomCreating() {
  // const classes = useStyles();

  return (
    <Box>
      <span>Please wait while the room is created...</span>
    </Box>
  );
}
